package com.example.agri_sense.ui.theme

import androidx.compose.ui.graphics.Color

val PremiumDarkGreen = Color(0xFF1B3B22)
val PremiumGold = Color(0xFFFFD700)

// Outdoor-readable surface: uses a deeper off-white to reduce glare wash-out
val PremiumSurface = Color(0xFFECF2ED)

// High-contrast text/label colors for sunny readability
val OnSurfaceStrong = Color(0xFF0D1F10)     // Near-black for primary labels
val OnSurfaceMedium = Color(0xFF2D4A31)     // Dark green for secondary text
val OnSurfaceSubtle = Color(0xFF4A6B4E)     // Muted green for hints / captions

val AgriGreenScreenBg = Color(0xFF2E7D32)
val AgriBeigeBg = Color(0xFFF5F5DC)
val LightGreyText = Color(0xFF424242)       // Darkened for contrast
val TagGreenBg = Color(0xFFD0E8D2)          // Deeper tag background
val StepBlue = Color(0xFF1565C0)            // Darker blue for contrast
