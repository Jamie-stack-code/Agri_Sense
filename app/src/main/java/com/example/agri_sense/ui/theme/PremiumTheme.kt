package com.example.agri_sense.ui.theme

import androidx.compose.ui.graphics.Color

// ── Core Brand ──────────────────────────────────────────────────────────────
val PremiumDarkGreen   = Color(0xFF1B3B22)
val PremiumGold        = Color(0xFFFFD700)

// ── Extended Premium Palette ─────────────────────────────────────────────────
val PremiumEmerald     = Color(0xFF2E8B57)   // vibrant mid-green for gradients
val PremiumForest      = Color(0xFF0D1F10)   // near-black deep forest hero bg
val PremiumDarkSurface = Color(0xFF152519)   // dark card / modal backgrounds
val PremiumAccentAmber = Color(0xFFFFA000)   // warm amber accent (alerts, badges)
val PremiumChartGreen  = Color(0xFF4CAF50)   // progress bars / chart fills
val PremiumTeal        = Color(0xFF00695C)   // ask-expert teal cards
val PremiumSkyBlue     = Color(0xFF0288D1)   // weather cards

// ── Card Gradient Stops ───────────────────────────────────────────────────────
val CardGradientSoil   = listOf(Color(0xFF1B3B22), Color(0xFF2E7D32))
val CardGradientCrop   = listOf(Color(0xFFE65100), Color(0xFFFFA000))
val CardGradientWeather= listOf(Color(0xFF01579B), Color(0xFF0288D1))
val CardGradientMarket = listOf(Color(0xFF4A148C), Color(0xFF7B1FA2))
val CardGradientPest   = listOf(Color(0xFFB71C1C), Color(0xFFE53935))
val CardGradientExpert = listOf(Color(0xFF004D40), Color(0xFF00695C))

// ── Surface / Text Tokens ────────────────────────────────────────────────────
val PremiumSurface  = Color(0xFFF4F7F5)  // Lighter surface for higher contrast
val OnSurfaceStrong = Color(0xFF0A140B)  // Near black
val OnSurfaceMedium = Color(0xFF1E3321)  // Darker green
val OnSurfaceSubtle = Color(0xFF334F37)  // Stronger contrast than before

val AgriGreenScreenBg = Color(0xFF2E7D32)
val AgriBeigeBg       = Color(0xFFF5F5DC)
val LightGreyText     = Color(0xFF424242)
val TagGreenBg        = Color(0xFFD0E8D2)
val StepBlue          = Color(0xFF1565C0)
